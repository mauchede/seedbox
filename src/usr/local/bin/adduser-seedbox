#!/bin/bash
set -e

usage() {
    echo "$0 [USER] [PASSWORD]" >&2
    exit 1
}

[[ -z $1 ]] && echo "Argument USER is missing." >&2 && usage
[[ -z $2 ]] && echo "Argument PASSWORD is missing." >&2 && usage

htpasswd -dbc /etc/seedbox/users $1 $2
